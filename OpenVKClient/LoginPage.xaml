<Page
    x:Class="OpenVKClient.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenVKClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox x:Name="UsernameTextBox" PlaceholderText="Username" Width="300" Margin="0,0,0,10"/>
            <PasswordBox x:Name="PasswordBox" PlaceholderText="Password" Width="300" Margin="0,0,0,20"/>
            <Button Content="Login" Click="LoginButton_Click" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>